const leapYears = function(year) {
    if (year % 4 && year % 400) {
        return 
    } else if (year % 4) {
        return
    } return
};

// Do not edit below this line
module.exports = leapYears;
